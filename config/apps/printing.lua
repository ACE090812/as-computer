-- Printing from Los Santos OS. Needs the as-printer resource running (printers, paper and ink, the tray).
-- Files (text and images), Notepad and MOT certificates get a Print button that lists the printers near the computer.
Config.Printing = {
  enabled = true,
}
